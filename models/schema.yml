version: 2

models:
  # ----------------------
  # Dimension Products
  # ----------------------
  - name: mrt_local_bike__dim_products
    description: "Dimension table for products."
    columns:
      - name: product_id
        description: "Unique identifier for each product."
        tests:
          - not_null
          - unique
      - name: product_name
        description: "Name of the product."
        tests:
          - not_null
      - name: brand_name
        description: "Brand of the product."
        tests:
          - not_null
      - name: model_year
        description: "Year of the product model."
      - name: price
        description: "Price of the product."
        tests:
          - not_null

  # ----------------------
  # Dimension Staffs
  # ----------------------
  - name: mrt_local_bike__dim_staffs
    description: "Dimension table for staff members."
    columns:
      - name: staff_id
        description: "Unique identifier for each staff member."
        tests:
          - not_null
          - unique
      - name: full_name
        description: "Full name of the staff member."
        tests:
          - not_null
      - name: store_id
        description: "Identifier of the store the staff belongs to."
        tests:
          - not_null
          - relationships:
              to: ref('mrt_local_bike__dim_stores')
              field: store_id
      - name: active
        description: "Indicates if the staff member is active or inactive."


  # ----------------------
  # Dimension Stores
  # ----------------------
  - name: mrt_local_bike__dim_stores
    description: "Dimension table for stores."
    columns:
      - name: store_id
        description: "Unique identifier for each store."
        tests:
          - not_null
          - unique
      - name: store_name
        description: "Name of the store."
        tests:
          - not_null
      - name: city
        description: "City of the store."
        tests:
          - not_null
      - name: state
        description: "State of the store."
        tests:
          - not_null

  # ----------------------
  # Fact Sales
  # ----------------------
  - name: mrt_local_bike__fact_sales
    description: "Fact table for sales."
    columns:
      - name: order_id
        description: "Identifier of the order."
        tests:
          - not_null
      - name: customer_id
        description: "Identifier of the customer."
        tests:
          - not_null
      - name: product_id
        description: "Identifier of the product."
        tests:
          - not_null
          - relationships:
              to: ref('mrt_local_bike__dim_products')
              field: product_id
      - name: store_id
        description: "Identifier of the store."
        tests:
          - not_null
          - relationships:
              to: ref('mrt_local_bike__dim_stores')
              field: store_id
      - name: staff_id
        description: "Identifier of the staff."
      - name: quantity
        description: "Quantity sold."

      - name: total_amount
        description: "Total amount of the sale."

      - name: order_status
        description: "Status of the order."
        tests:
          - accepted_values:
              values:
                [
                  "Registred",
                  "Confirmed",
                  "Cancelled",
                  "Shipped",
                  "Unknown status",
                ]

  # ----------------------
  # Fact Stocks
  # ----------------------
  - name: mrt_local_bike__fact_stocks
    description: "Fact table for stock levels."
    columns:
      - name: store_id
        description: "Identifier of the store."
        tests:
          - not_null
          - relationships:
              to: ref('mrt_local_bike__dim_stores')
              field: store_id
      - name: product_id
        description: "Identifier of the product."
        tests:
          - not_null
          - relationships:
              to: ref('mrt_local_bike__dim_products')
              field: product_id
      - name: quantity
        description: "Quantity of product in stock."

